# Building

To build execute the following

```console
uv build
```

To test execute the following

```console
uv run pytest
```

To get kernel version and OS info

```console
cat /etc/os-release
uname -a
```
